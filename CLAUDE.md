# RAINMOJO SO Lite

ปลั๊กอินสำหรับผู้เรียนคอร์ส BIQDADDY AI Visibility ใช้ตรวจและปรับเว็บไซต์ให้ระบบ AI
อย่าง ChatGPT, Claude, Gemini และ Google AI Overviews มองเห็นและหยิบไปอ้างอิง
รุ่นนี้เป็นรุ่น Lite ที่ทำขึ้นเพื่อใช้ในห้องเรียนและเวิร์กช็อปโดยเฉพาะ

ไฟล์นี้คือเอกสารกลางของปลั๊กอิน เนื้อหาเหมือนกันทุกตัวอักษรกับ CLAUDE.md และ GEMINI.md
เพื่อให้ทุก host อ่านคำอธิบายชุดเดียวกัน ถ้าจะแก้ ให้แก้ AGENTS.md แล้วรัน build.py ใหม่

## ปลั๊กอินนี้ทำอะไร

AISO (AI Search Optimization) คือการทำให้ระบบ AI เข้าถึงเนื้อหาของเราได้ เข้าใจว่าเราเป็นใคร
และเลือกหยิบข้อความของเราไปตอบผู้ใช้ แบ่งเป็นสองงานย่อยคือ AEO ที่ทำให้ถูกหยิบไปตอบสั้น ๆ
และ GEO ที่ทำให้ LLM หยิบไปอ้างอิงและเรียบเรียงใหม่

ปลั๊กอินนี้ครอบคลุมงานทั้งสองฝั่ง ตั้งแต่ตรวจว่าบอทเข้าเว็บได้ไหม ไปจนถึงเขียนบทความ
แล้วส่งขึ้น WordPress

## โครงสร้าง

- **43 สกิล** คือคำสั่งเดี่ยว ทำงานอย่างเดียวจบ
- **17 agent** คือตัวคุมที่เรียกสกิลหลายตัวต่อกันตามลำดับที่ถูกต้อง

ผู้เรียนใช้ agent เป็นหลักเพราะไม่ต้องจำลำดับ ส่วนสกิลเดี่ยวใช้ตอนอยากทำเฉพาะจุด

## กติกาที่ทุกสกิลต้องทำตาม

1. **รัน `new-workspace-lite` ก่อนเสมอ** เพื่อสร้างโครง `work/{domain}/`
2. ผลลัพธ์ทุกไฟล์อยู่ใต้ `work/{domain}/` ครั้งแรกลง `00-baseline/` ครั้งถัดไปลง `01-current/`
3. รายงานรวมอยู่ที่ `work/{domain}/04-reports/`
4. เขียนไฟล์เสร็จ ต้องเพิ่มบรรทัดลง `work/{domain}/CHANGELOG.md`

## สกิล 43 ตัว

### กลุ่มที่ 1 เริ่มต้นและจบงาน

| สกิล | ใช้ทำอะไร |
|---|---|
| `rainmojo-so-lite` | **สารบัญของปลั๊กอิน** ถามว่ามีอะไรบ้างหรือควรใช้ตัวไหน ให้เรียกตัวนี้ · ไม่ตรวจเว็บเอง |
| `new-workspace-lite` | **รันตัวนี้ก่อนเสมอ** สร้างโครงโฟลเดอร์ให้ผลลัพธ์จากสกิลอื่นมีที่อยู่ที่แน่นอน |
| `report-lite` | รวมผลตรวจทั้งหมดเป็นรายงาน HTML หน้าเดียวที่ส่งลูกค้าได้ |
| `compare-lite` | เทียบผลสแกนสองรอบ บอกว่าอะไรดีขึ้นจริง อะไรเป็นแค่ noise |
| `so-present-lite` | ชั้นแสดงผล: การ์ดสรุป Tier 1 ในแชตบน host ใดก็ได้ (widget, artifact, Mermaid, ASCII, Markdown) และ 7 โหมดคำตอบ ค่าบนการ์ดคัดลอกจากไฟล์ผลลัพธ์ ไม่มีคะแนน |

### กลุ่มที่ 2 ตรวจฝั่ง AI

| สกิล | ใช้ทำอะไร |
|---|---|
| `aiso-crawlers-lite` | ตรวจ robots.txt, meta robots และ HTTP header ว่าบอทของค่าย AI เข้าถึงได้หรือถูกบล็อก |
| `aiso-llmstxt-lite` | ตรวจและสร้างไฟล์ llms.txt |
| `aiso-schema-lite` | ตรวจ JSON-LD ที่มีอยู่บนหน้าเว็บ ว่าครบและถูกต้องในมุมที่ AI ใช้ |
| `aiso-technical-lite` | ตรวจฝั่งเทคนิคที่กระทบ AI โดยเฉพาะ เช่น เนื้อหาอยู่ใน HTML ดิบหรือขึ้นด้วย JavaScript |
| `aiso-content-lite` | ประเมินคุณภาพเนื้อหาตามกรอบ E-E-A-T 4 มิติ แบบเชิงคุณภาพ |
| `aiso-citability-lite` | ตรวจว่าย่อหน้าอยู่ในรูปที่ AI หยิบไปอ้างได้ง่ายแค่ไหน พร้อมข้อเสนอวิธีเขียนใหม่ |
| `aiso-platform-lite` | เช็กลิสต์ความพร้อม 11 การ์ดของ 10 แพลตฟอร์มคำตอบ AI (ChatGPT, Perplexity, Claude, Google AI Overviews, Gemini, DeepSeek, Grok, Qwen, Meta AI, Copilot, Apple Intelligence) ผูกกับทะเบียนบอท ไม่มีคะแนน |
| `aiso-brand-mentions-lite` | นับการถูกพูดถึงบน YouTube, Reddit, Wikipedia และ LinkedIn และสุ่มถาม AI จริงเป็นตารางคำถามกับ 6 มิติแบบจำนวนนับ |
| `aiso-agent-readiness-lite` | เช็กลิสต์ 14 ข้อว่า AI agent (ChatGPT agent, Claude in Chrome) มองเห็นและกดใช้เว็บได้ไหม จาก HTML และ accessibility tree พร้อมวิธีแก้ระดับโค้ด |

### กลุ่มที่ 3 ตรวจฝั่ง SEO ดั้งเดิม

| สกิล | ใช้ทำอะไร |
|---|---|
| `seo-website-overview-lite` | สแกนภาพรวมทั้งไซต์ครั้งแรก ใช้เป็นจุดตั้งต้นของงานอื่น |
| `technical-audit-lite` | ตรวจ SEO พื้นฐาน ความเร็ว มือถือ HTTPS และการเก็บหน้าเข้าระบบ |
| `seo-heading-matrix-lite` | ตรวจโครงหัวข้อ H1 ถึง H3 ว่าเรียงถูกและ AI อ่านรู้เรื่องไหม |
| `meta-basics-lite` | เขียน title และ meta description ให้ความยาวถูกต้อง นับตัวอักษรไทยได้ |
| `seo-schema-lite` | สร้าง schema markup 4 แบบ Organization, LocalBusiness, FAQPage, Article |
| `seo-internal-linking-lite` | วางแผนลิงก์ภายในแบบ hub และ spoke |
| `seo-image-optimizer-lite` | จัดรูปให้เบาและอ่านออก แปลง WebP ตั้งชื่อไฟล์ เขียน alt และตรวจว่าภาพถูก index ได้ |
| `sitemap-architecture-lite` | มาตรฐาน sitemap ที่ถูกต้องและอ่านได้เมื่อเว็บโต: sitemap index, การแบ่งไฟล์, lastmod, hreflang |

### กลุ่มที่ 4 หาคำค้นและหาช่องว่าง

| สกิล | ใช้ทำอะไร |
|---|---|
| `seo-keyword-research-lite` | หาคำค้นด้วยวิธี Google Suggest รองรับภาษาไทย |
| `seo-keyword-clustering-lite` | จับคำค้นที่ความหมายใกล้กันมารวมเป็นกลุ่ม |
| `seo-content-gap-analysis-lite` | หาคำถามที่ลูกค้าถามแต่เว็บเรายังไม่ตอบ |
| `seo-competitor-lite` | ระบุคู่แข่งที่ AI เอ่ยชื่อจริง แล้วถอดว่าเขาถูกอ้างเพราะอะไร |
| `seo-topical-authority-lite` | วางโครง 1 หัวข้อหลักบวก 10 หัวข้อย่อย |

### กลุ่มที่ 5 ตรวจและปรับเนื้อหา

| สกิล | ใช้ทำอะไร |
|---|---|
| `seo-content-audit-lite` | ตรวจเนื้อหาที่มีอยู่ตามกรอบ E-E-A-T และ YMYL |
| `seo-content-enhancement-lite` | ปรับเนื้อหาเดิมให้ AI หยิบง่ายขึ้น โดยไม่ต้องเขียนใหม่ทั้งหน้า |
| `seo-content-strategy-lite` | วางแผนว่าจะเขียนอะไรก่อนหลัง |
| `seo-video-lite` | ทำวิดีโอให้ถูก index: preflight, thumbnail, hosting, คำบรรยาย และตรวจผลหลังเผยแพร่ |

### กลุ่มที่ 6 นอกเว็บและท้องถิ่น

| สกิล | ใช้ทำอะไร |
|---|---|
| `seo-local-seo-lite` | Google Business Profile ความสม่ำเสมอของชื่อที่อยู่เบอร์ และ schema ท้องถิ่น |
| `seo-backlink-strategy-lite` | หาโอกาสได้ลิงก์จากเว็บอื่น และประเมินว่าลิงก์ไหนควรเอาไหนควรเลี่ยง |

### กลุ่มที่ 6b ย้ายเว็บและพิสูจน์ผล

| สกิล | ใช้ทำอะไร |
|---|---|
| `seo-migration-lite` | วางแผนย้าย URL โดเมน โฮสต์ หรือ CMS โดยไม่เสียของเดิม: แผนที่ URL เก่าไปใหม่ ลำดับการย้าย และหลุมพราง redirect |
| `seo-experiment-lite` | ทดสอบว่าการแก้หน้าเว็บทำให้อันดับดีขึ้นจริงไหม: แบ่งกลุ่มหน้า มี holdout เปลี่ยนทีละอย่าง |

### กลุ่มที่ 7 โรงงานเนื้อหา ให้แบบเต็มไม่ตัดฟีเจอร์

| สกิล | ใช้ทำอะไร |
|---|---|
| `new-client-lite` | **รันก่อนใช้กลุ่มนี้** สร้างโฟลเดอร์ลูกค้าใหม่พร้อมไฟล์ตั้งต้น |
| `seo-content-creation-lite` | เขียนบทความจริงตั้งแต่โครงถึงเนื้อหาเต็ม ตามบรีฟที่ให้ |
| `content-production-lite` | ประกอบบทความเป็นแพ็กเกจพร้อมขึ้นเว็บ ทั้งเนื้อหา รูป ลิงก์ และ meta |
| `wp-rest-ops-lite` | คุยกับ WordPress ผ่าน REST API รวมวิธีแก้เมื่อโดนไฟร์วอลล์กั้น |
| `wp-content-publisher-lite` | ส่งแพ็กเกจขึ้น WordPress พร้อมอัปโหลดรูปและตั้งหมวดหมู่ |

### กลุ่มที่ 8 เส้นทางง่ายสำหรับงานชิ้นเดียว

| สกิล | ใช้ทำอะไร |
|---|---|
| `content-draft-lite` | เขียนแพ็กเกจบทความในเครื่อง พร้อม meta, schema, บรีฟภาพ และ checklist |
| `wp-connect-lite` | ตรวจว่าเชื่อม WordPress ได้จริงก่อน อ่าน credential จาก `.env` เท่านั้น |
| `wp-publish-draft-lite` | ส่งบทความขึ้น WordPress **เป็น draft เท่านั้น** อัปภาพพร้อม alt text แล้วดึงกลับมาตรวจ |

## agent 17 ตัว

### ตรวจใหญ่

| agent | ใช้ทำอะไร |
|---|---|
| `so-audit-lite` | ตรวจครบทั้ง SEO และ AISO ในรอบเดียว รวมเป็นรายงานเดียว |
| `seo-audit-lite` | ตรวจ SEO ทั้งเว็บ 6 ด้านตามลำดับ |
| `aiso-audit-lite` | ตรวจฝั่ง AI ครบทั้ง 9 ด้าน รวม AI agent readiness แล้วเรียงงานตามความสำคัญ |

### สาย AI

| agent | ใช้ทำอะไร |
|---|---|
| `aiso-ai-visibility-lite` | วัดการมองเห็นบน AI แบบต่อเนื่อง แล้วเทียบสองรอบให้ |
| `aiso-content-review-lite` | ตรวจคุณภาพเนื้อหาให้ AI หยิบไปอ้างอิงได้ |
| `aiso-platform-analysis-lite` | ตรวจความพร้อมแยกรายแพลตฟอร์ม ครบ 11 การ์ดของ 10 แพลตฟอร์ม |
| `aiso-schema-audit-lite` | ตรวจ JSON-LD ที่มี แล้วสร้างส่วนที่ขาด |
| `aiso-technical-audit-lite` | ตรวจเทคนิคทั้งมุม SEO พื้นฐานและฝั่ง AI |

### สาย SEO

| agent | ใช้ทำอะไร |
|---|---|
| `seo-technical-lite` | ตรวจเทคนิคโดยเน้นมุม SEO ดั้งเดิม |
| `seo-onpage-lite` | meta, schema, โครงหัวข้อ และลิงก์ภายใน |
| `seo-keyword-lite` | หาคำค้น จัดกลุ่ม แล้วหาช่องว่าง |
| `seo-content-lite` | กลยุทธ์เนื้อหาตั้งแต่วางแผนถึงเขียนและปรับ |
| `seo-local-lite` | งานฝั่งพื้นที่ |
| `seo-offpage-lite` | คู่แข่งและแผนสร้างลิงก์ |
| `seo-disavow-auditor-lite` | ประเมิน backlink เบื้องต้น แล้วชี้ทางไปปลั๊กอิน disavow |

### สายลงมือ

| agent | ใช้ทำอะไร |
|---|---|
| `content-production-orchestrator-lite` | คุมสายผลิตเนื้อหาตั้งแต่ตั้ง workspace จนขึ้น WordPress เป็น draft |
| `remediation-orchestrator-lite` | อ่านผลตรวจแล้วทำแผนแก้เป็นตาราง ไม่ลงมือแก้เว็บเอง |

## วิธีเรียกใช้

พิมพ์เป็นภาษาปกติได้เลย ไม่ต้องจำคำสั่ง ระบบจะเลือกสกิลหรือ agent ให้เอง ตัวอย่าง

- "ตรวจ robots.txt ของ example.com ว่า AI crawler ตัวไหนเข้าได้บ้าง"
- "ตรวจ AI visibility ของ example.com ให้ครบทุกด้าน"
- "หาคำค้นภาษาไทยเกี่ยวกับ คลินิกทันตกรรม แล้วจัดกลุ่มให้ด้วย"
- "ดูว่าคู่แข่งของเราถูก AI พูดถึงเพราะอะไร"
- "เขียนบทความเรื่องนี้แล้วส่งขึ้นเว็บเป็น draft"

ถ้าอยากบังคับให้ใช้ตัวใดตัวหนึ่ง ให้พิมพ์ชื่อลงไปตรง ๆ เช่น "ใช้สกิล aiso-citability-lite กับหน้านี้"

## ขอบเขตของรุ่น Lite

- ผลลัพธ์เป็นระดับเชิงคุณภาพ ดี พอใช้ ต้องแก้ **ไม่มีคะแนนรวมและไม่มีสูตรถ่วงน้ำหนัก**
  ข้อนี้ใช้กับทุกสกิล ยกเว้นกลุ่มที่ 7 ที่ให้แบบเต็ม
- `aiso-platform-lite` ตรวจครบ 10 แพลตฟอร์ม แต่เป็นเช็กลิสต์ต่อการ์ด ไม่มีคะแนน แพลตฟอร์มที่ไม่ประกาศชื่อบอท (Grok, DeepSeek, Qwen) ข้อการเข้าถึงลงว่า ตรวจไม่ได้
- ทุกสกิลและ agent ปิดงานด้วยการ์ด Tier 1 ในแชตผ่าน `so-present-lite` การ์ดแสดงจำนวนข้อที่ผ่านต่อจำนวนข้อที่ตรวจ ไม่มีคะแนนรวม
- ไม่มี `aiso-task-completion` (การวัด Task Completion Rate ด้วย agent จริง มีต้นทุน LLM) และไม่มี axe-core, settle probe, legal note ใน `aiso-agent-readiness-lite`
- ไม่มี emoji ในไฟล์ใดของปลั๊กอิน ในแชตใช้แถบ ASCII และ glyph ที่อนุญาต ในรายงาน HTML ใช้ไอคอน SVG
- ไม่มีสกิลตรวจ cannibalization ของ WordPress สกิลกลุ่มนั้นอยู่ในปลั๊กอิน `wp-seo-remediation-lite`
- **ไม่มีชุดแก้เว็บ WordPress อัตโนมัติ** เช่น แก้ canonical, ตั้ง redirect, แทรก CTA
  หรือแก้เนื้อหาเดิมทีละหลายหน้า ทางที่ให้คือส่งขึ้นเป็นฉบับร่างให้คนกดเผยแพร่เอง

## ข้อควรระวัง

1. สกิลส่วนใหญ่อ่านข้อมูลสาธารณะจากหน้าเว็บเท่านั้น ไม่ต้องใช้ API key และไม่ต้องล็อกอิน
   ส่วนกลุ่มที่ 7 และ 8 ที่คุยกับ WordPress ต้องใช้ credential ของเว็บตัวเอง
2. **credential ต้องอยู่ในไฟล์ `.env` เท่านั้น ห้ามพิมพ์ลงในบทสนทนา**
   ใช้ WordPress application password ไม่ใช่รหัสผ่านที่ใช้ล็อกอินปกติ เพราะถอนได้ทีละอัน
   และต้องใส่ `.env` ไว้ใน `.gitignore` ถ้าโฟลเดอร์งานอยู่ใน git
3. ผลลัพธ์ทุกชิ้นเป็นข้อเสนอแนะ ไม่ใช่การเปลี่ยนแปลงเว็บไซต์ ผู้เรียนเป็นคนตัดสินใจนำไปใช้เอง
4. ปลั๊กอินชุดนี้เป็นรุ่นสำหรับเรียน ไม่ได้ออกแบบมาให้ใช้กับงานลูกค้าที่มีความเสี่ยงสูง

## ไฟล์ประกอบแยกตาม host

| Host | ไฟล์ที่ host อ่าน |
|---|---|
| Claude Code, Claude Desktop, Cowork | `.claude-plugin/plugin.json` และ `CLAUDE.md` |
| OpenAI Codex, GPT desktop | `.codex-plugin/plugin.json` และ `AGENTS.md` |
| Gemini CLI | `gemini-extension.json` และ `GEMINI.md` |
| Google Antigravity | `plugin.json` ที่ราก |

ทุก host อ่านสกิลจากโฟลเดอร์ `skills/` ชุดเดียวกัน ไม่มีการทำสำเนาแยกราย host
